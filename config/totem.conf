# Diagnostic firmware keeps optional pointing HID disabled.
#
# Be explicit about outputs. Without CONFIG_ZMK_USB, the application firmware
# can boot without exposing a USB HID keyboard even though the UF2 bootloader
# still appears as a USB storage device.
CONFIG_ZMK_USB=y
CONFIG_ZMK_BLE=y
